# Copy this to .env.local for local development.
# For production, set this as a GitHub Actions secret named OPENAI_API_KEY
# (the workflow maps it to NEXT_PUBLIC_OPENAI_API_KEY at build time).
NEXT_PUBLIC_OPENAI_API_KEY=sk-your-key-here
